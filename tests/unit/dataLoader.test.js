'use strict'
/* eslint-env jest */

import { toDaily } from '../../src/utils/dataLoader'

describe('loader works fine', () => {

    test('first test', () => {
    expect('value').toEqual('value')
  })

})